# Configuration for the Chatbot Backend

# Set to 'true' to enable real integration with ts-ai-agent
# Set to 'false' to use mock responses (default)
USE_REAL_AGENT=false

# Absolute or relative path to the ts-ai-agent repository
# Required if USE_REAL_AGENT=true
TS_AGENT_PATH=../ts-ai-agent
